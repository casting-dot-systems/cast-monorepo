[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "cast-git"
version = "0.1.0"
description = "Git wrapper utilities for Cast (future use)"
requires-python = ">=3.10"
dependencies = []

[project.optional-dependencies]
dev = [
    "pytest>=8.3.0"
]